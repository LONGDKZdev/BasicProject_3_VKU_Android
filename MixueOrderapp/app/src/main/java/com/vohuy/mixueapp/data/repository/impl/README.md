# Repository implementations

- `SupabaseImageRepository`: 100% image storage via Supabase Storage (public bucket).

Firebase repositories remain in `data/repository/` for now and can be moved later.

