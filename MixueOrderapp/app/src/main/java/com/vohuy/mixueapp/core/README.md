# core module (planned)

This package will host cross-cutting concerns:
- constants
- result types
- error handling
- extensions
- config (BuildConfig keys)

Currently some of these are in `utils/`. In later phases we can move them without changing logic.

